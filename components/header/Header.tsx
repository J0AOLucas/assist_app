import React from 'react';
import { View, Text } from 'react-native';
import { styles } from './Styles';

export const Header = () => {
  return (
    <View>
      <Text>Header</Text>
    </View>
  );
};